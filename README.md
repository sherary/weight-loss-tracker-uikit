# Weight Loss Tracker

An iOS weight tracking app built with UIKit. 
History list, weekly trend charts, dynamic add/edit, persistent storage.

## Requirements
- Xcode 16+
- iOS 18+

## Architecture
- UIKit (programmatic, no Storyboards)
- MVC with a shared `WeightStore` service
- UserDefaults persistence (SQL planned)

## Status
- Beta